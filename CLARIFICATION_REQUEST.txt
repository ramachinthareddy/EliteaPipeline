I need clarification before generating code.

Please confirm the following so I can proceed:

1) Where should I save the input/metadata (target_branch, bdd features, repo link)? Choose one:
   - a) save in a variable inside generated code
   - b) save in a configuration file (specify filename and format, e.g., .github/config.yml, config/registration-tests.json)
   - c) save in a script (specify path)
   - d) use it only for the intended git operation (create branch + PR) and do not persist

2) Git permissions and process:
   - Do you want me to create a new branch in the repository https://github.com/ramachinthareddy/EliteaPipeline? (I will create one branch per feature task as requested.)
   - Target branch to open PR against: "EliteayTest" (confirmed). Please confirm I should name the feature branch like: "feature/amz-registration-<short-scenario-id>".
   - Do you provide credentials/token or should I proceed with instructions only (I cannot push without credentials)?

3) Code generation details:
   - Confirm language/framework: Java + Selenium + TestNG + Page Object Model (POM). (If any variations, please state.)
   - Do you want all scenarios implemented in a single TestNG test class or one test class per feature/area (e.g., RegistrationNameTests, ContactOtpTests, PasswordTests, OTPTests)?
   - Do you have a preferred package structure? (example: com.elitea.pages, com.elitea.tests, com.elitea.utils)
   - Any existing base test classes/utilities in the repo to reuse? If so, point me to their paths.

4) Test data and environment:
   - For OTP/email/SMS tests requiring external systems, do you have test inbox/gateways and credentials available? Please provide endpoints or indicate whether to stub/mock those interactions.
   - For scenarios requiring DB inspection (AMZ_TC_018) or staging environment privileges, confirm whether those steps should be represented as placeholder assertions or require integration with provided staging access.

5) Acceptance criteria for generated step definitions:
   - Should the code include realistic Selenium locators for elements (IDs/XPaths/CSS) using placeholders (e.g., By.id("fullName"))? If exact locators exist in the repo, provide references.
   - Should I include TestNG annotations, retry logic, and reporting hooks? Any specific style guide or linters to follow?

Once you confirm the above, I will:
 - Check the repository for existing code and whether it is up to date.
 - Create a feature branch (one per feature/task) and add the generated Java Selenium + TestNG POM-based step definitions.
 - Open a PR against EliteayTest (no direct pushes to main).

Please reply with choices for items 1-5 and provide any credentials or repo paths needed for direct GitHub operations, or indicate you want only the code payload for manual application.